#ifndef BOARD_ENDPOINTS_H
#define BOARD_ENDPOINTS_H

#include <ESPAsyncWebServer.h>

// Setup board information endpoint
void setupBoardEndpoints(AsyncWebServer& server);

#endif // BOARD_ENDPOINTS_H
